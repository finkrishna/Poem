# SpaceXAI (xAI) — required for the factory server
# https://console.x.ai  (set a monthly spend cap there)
XAI_API_KEY=
XAI_MODEL=grok-4.5
HOST=0.0.0.0
PORT=8765
# Shared public host: readers per visitor per hour
MAX_PER_HOUR=10
