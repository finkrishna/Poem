# Hugging Face Inference (preferred — Qwen 2.5 72B via Novita)
# Local: hf auth login. Hosted: Space secret HF_TOKEN.
# HF_TOKEN=
HF_MODEL=Qwen/Qwen2.5-72B-Instruct:novita

# SpaceXAI fallback if HF Inference fails
# https://console.x.ai  (set a monthly spend cap there)
XAI_API_KEY=
XAI_MODEL=grok-4.5
HOST=0.0.0.0
PORT=8765
# Shared public host: readers per visitor per hour
MAX_PER_HOUR=10
