services:
  - type: web
    name: poem-reader
    runtime: docker
    healthCheckPath: /api/health
    envVars:
      - key: HOST
        value: 0.0.0.0
      - key: XAI_MODEL
        value: grok-4.5
      - key: MAX_PER_HOUR
        value: "10"
      - key: XAI_API_KEY
        sync: false
